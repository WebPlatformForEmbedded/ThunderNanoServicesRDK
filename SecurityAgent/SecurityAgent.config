set(startmode ${PLUGIN_SECURITYAGENT_STARTMODE})

if(PLUGIN_SECURITYAGENT_STARTUPORDER)
    set (startuporder ${PLUGIN_SECURITYAGENT_STARTUPORDER})
endif()

map()
    kv(acl acl.json)
end()
ans(configuration)
